local colors = require("colors")

sbar.bar({
  position      = "top",
  height        = 40,
  y_offset      = 10,
  margin        = 10,
  color         = colors.transparent,
  corner_radius = 10,
  padding_left  = 12,
  padding_right = 12,
})
